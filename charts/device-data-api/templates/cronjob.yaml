{{- if .Values.cronJob.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "device-data-api.fullname" . }}-load-dd
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "device-data-api.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.cronJob.schedule | quote}}
  concurrencyPolicy: "Forbid"
  jobTemplate:
    spec:
      template:
        metadata:
          annotations:
          {{- with .Values.podAnnotations }}
            {{- toYaml . | nindent 8 }}
          {{- end }}
          labels:
            {{- include "device-data-api.selectorLabels" . | nindent 12 }}
        spec:
          initContainers:
          - name: edmunds-vehicles-sync
            securityContext:
              {{- toYaml .Values.securityContext | nindent 14 }}
            image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
            command: ['sh', '-c', "/device-data-api edmunds-vehicles-sync"]
            envFrom:
            - configMapRef:
                name: {{ include "device-data-api.fullname" . }}-config
            - secretRef:
                name: {{ include "device-data-api.fullname" . }}-secret
          - name: search-sync-dds
            securityContext:
              {{- toYaml .Values.securityContext | nindent 14 }}
            image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
            command: ['sh', '-c', "/device-data-api search-sync-dds"]
            envFrom:
            - configMapRef:
                name: {{ include "device-data-api.fullname" . }}-config
            - secretRef:
                name: {{ include "device-data-api.fullname" . }}-secret
          containers:
          - name: completed
            image: busybox
            command: ['sh', '-c', 'echo "edmunds-vehicles-sync and search-sync-dds completed"']
          restartPolicy: OnFailure
{{ end }}          